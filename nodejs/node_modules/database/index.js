let phrases;

exports.connect = function() {
  phrases = require("./en.json")
}

exports.getPhrase = function(name) {
  if(!phrases[name]) {
    throw new Error ("Database don't have this phrase " + name)
  }

  return phrases[name];
}